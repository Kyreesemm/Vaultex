[package]
name = "vaultex-app"
version = "0.1.0"
description = "Vaultex desktop application"
authors = ["Vaultex contributors"]
edition = "2021"

[lib]
name = "vaultex_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
vaultex-core = { path = "../../crates/vaultex-core" }
getrandom = "0.2"
serde = { version = "1", features = ["derive"] }
